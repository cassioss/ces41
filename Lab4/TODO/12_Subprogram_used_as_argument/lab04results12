program AnaliseDeTexto {{{

global {
	int a;
}

logic function EhBooleano()
statements {
	return true;
}

int function FuncaoZero(logic bool)
statements {
	return 0;
}

main
statements {
	a = FuncaoZero(EhBooleano());
}

}}}


  =========================
  = TABELA  DE  SIMBOLOS: =
  =========================

Classe 5:
  (a, IDVAR, INTEIRO, 1, 1, ##global)
  (##global, IDGLOB, NAOVAR, 0, 0)

Classe 8:
  (##main, IDPROG, NAOVAR, 0, 0, ##global)
  (FuncaoZero, IDFUNC, INTEIRO, 0, 0, ##global)

Classe 14:
  (bool, IDVAR, LOGICO, 1, 1, FuncaoZero)

Classe 22:
  (EhBooleano, IDFUNC, LOGICO, 0, 0, ##global)
